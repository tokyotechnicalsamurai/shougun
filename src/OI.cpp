#include "OI.h"
#include "RobotMap.h"

OI::OI() : joystick(JOYSTICK)
{
	/*
	JoystickButton AButton = new JoystickButton(&joystick,A_BUTTON),
				   BButton = new JoystickButton(&joystick,B_BUTTON),
				   XButton = new JoystickButton(&joystick,X_BUTTON);

	*/

	// Process operator interface input here.
}
